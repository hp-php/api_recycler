<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.api_recycler_demo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.API_Recycler_Demo"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".api.activity.GetAllDisease"
            android:exported="false" />
        <activity
            android:name=".api.activity.ListPackages"
            android:exported="false" />
        <activity
            android:name=".api.activity.ListTest"
            android:exported="false" />
        <activity
            android:name=".api.activity.GetAllHabits"
            android:exported="false" />
        <activity
            android:name=".api.activity.GetAll_RiskAreas"
            android:exported="false" />
        <activity
            android:name=".api.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>